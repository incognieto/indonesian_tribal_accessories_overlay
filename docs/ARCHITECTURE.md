# System Architecture Diagrams

## 1. High-Level Pipeline Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│                          INPUT IMAGE/VIDEO                          │
│                                                                     │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
        ┌────────────────────────────────────────┐
        │   Haar Cascade Multi-Scale Detection   │
        │   • frontalface_default (primary)      │
        │   • frontalface_alt/alt2/tree          │
        │   • profileface (side faces)           │
        │   • eye, nose, mouth (features)        │
        └─────────────┬──────────────────────────┘
                      │
                      │ Candidate ROIs (x,y,w,h)
                      ▼
        ┌────────────────────────────────────────┐
        │     ORB Feature Extraction             │
        │   • Detect 500 keypoints               │
        │   • Extract 32-byte descriptors        │
        │   • Handle zero-keypoint fallback      │
        └─────────────┬──────────────────────────┘
                      │
                      │ Descriptors (Nx32)
                      ▼
        ┌────────────────────────────────────────┐
        │    Bag-of-Visual-Words Encoding        │
        │   • Assign to k=256 visual words       │
        │   • Build histogram (L1-normalized)    │
        │   • StandardScaler normalization       │
        └─────────────┬──────────────────────────┘
                      │
                      │ BoVW Feature Vector (256D)
                      ▼
        ┌────────────────────────────────────────┐
        │       SVM Binary Classification        │
        │   • Face vs Non-Face                   │
        │   • Decision function scoring          │
        │   • Threshold filtering                │
        └─────────────┬──────────────────────────┘
                      │
                      │ Validated Faces + Scores
                      ▼
        ┌────────────────────────────────────────┐
        │      Post-processing (NMS + Refine)    │
        │   • Non-Maximum Suppression (IoU 0.3)  │
        │   • Landmark detection (eyes, nose)    │
        │   • Pose estimation (rotation angle)   │
        └─────────────┬──────────────────────────┘
                      │
                      │ Final Face Detections
                      ▼
        ┌────────────────────────────────────────┐
        │       Accessory Overlay System         │
        │   • Hat (scaled & rotated)             │
        │   • Earrings (left & right)            │
        │   • Nose piercing (landmark-based)     │
        │   • Face tattoo (with opacity)         │
        │   • Alpha blending (RGBA)              │
        └─────────────┬──────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│                    OUTPUT IMAGE/VIDEO WITH OVERLAYS                 │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 2. Training Pipeline

```
┌──────────────────┐     ┌──────────────────┐
│  Positive Images │     │ Negative Images  │
│  (Faces)         │     │  (Non-faces)     │
└────────┬─────────┘     └────────┬─────────┘
         │                        │
         └────────┬───────────────┘
                  │
                  ▼
         ┌─────────────────────┐
         │  Haar ROI Extraction│
         │  (if full images)   │
         └──────────┬──────────┘
                    │
                    ▼
         ┌─────────────────────┐
         │  Train/Val/Test     │
         │  Split (70/15/15)   │
         └──────────┬──────────┘
                    │
         ┌──────────┴──────────┐
         │                     │
         ▼                     ▼
┌─────────────────┐   ┌─────────────────┐
│ ORB Extraction  │   │ ORB Extraction  │
│ (Train Set)     │   │ (Val/Test)      │
└────────┬────────┘   └────────┬────────┘
         │                     │
         ▼                     │
┌─────────────────┐            │
│ k-means         │            │
│ Clustering      │            │
│ → Codebook.pkl  │            │
└────────┬────────┘            │
         │                     │
         ▼                     ▼
┌─────────────────┐   ┌─────────────────┐
│ BoVW Encoding   │   │ BoVW Encoding   │
│ (Train)         │   │ (Val/Test)      │
└────────┬────────┘   └────────┬────────┘
         │                     │
         ▼                     │
┌─────────────────┐            │
│ Fit Scaler      │            │
│ → scaler.pkl    │            │
└────────┬────────┘            │
         │                     │
         ▼                     ▼
┌─────────────────┐   ┌─────────────────┐
│ Normalize       │   │ Normalize       │
│ (Train)         │   │ (Val/Test)      │
└────────┬────────┘   └────────┬────────┘
         │                     │
         ▼                     │
┌─────────────────┐            │
│ Train SVM       │            │
│ (Grid Search CV)│            │
│ → svm_face.pkl  │            │
└────────┬────────┘            │
         │                     │
         │                     ▼
         │            ┌─────────────────┐
         │            │ Evaluate        │
         │            │ • Accuracy      │
         │            │ • Precision     │
         │            │ • Recall        │
         │            │ • F1 Score      │
         │            │ • ROC-AUC       │
         │            └─────────────────┘
         │
         ▼
  ┌──────────────┐
  │ Save Models  │
  │ & Config     │
  └──────────────┘
```

---

## 3. Inference Pipeline (Real-time)

```
       ┌────────────────┐
       │  Webcam Frame  │
       │  or Image      │
       └───────┬────────┘
               │
               ▼
    ┌──────────────────────┐
    │ Detect Faces (Haar)  │
    │ • frontalface        │
    │ • profileface        │
    └──────────┬───────────┘
               │
               │ For each candidate:
               ▼
    ┌──────────────────────┐
    │ Extract ROI          │
    │ → Grayscale 128×128  │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │ ORB Descriptors      │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │ BoVW Encode          │
    │ (use codebook.pkl)   │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │ Normalize            │
    │ (use scaler.pkl)     │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │ SVM Predict          │
    │ • decision_function  │
    │ • threshold filter   │
    └──────────┬───────────┘
               │
               │ Keep if score > 0
               ▼
    ┌──────────────────────┐
    │ Valid faces          │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │ NMS (IoU < 0.3)      │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │ Detect Features      │
    │ • Eyes (for rotation)│
    │ • Nose (for piercing)│
    └──────────┬───────────┘
               │
               │ For each face:
               ▼
    ┌──────────────────────┐
    │ Calculate Positions  │
    │ • Hat: top + rotate  │
    │ • Ears: ±45% width   │
    │ • Nose: 58% height   │
    │ • Tattoo: cheek      │
    └──────────┬───────────┘
               │
               ▼
    ┌──────────────────────┐
    │ Alpha Blend Overlays │
    │ • Resize accessories │
    │ • Rotate (if needed) │
    │ • Blend RGBA         │
    └──────────┬───────────┘
               │
               ▼
       ┌────────────────┐
       │ Output Frame   │
       │ with Overlays  │
       └────────────────┘
```

---

## 4. Module Dependencies

```
                    app.py (CLI)
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
    dataset.py     train.py        infer.py
        │               │               │
        │               ├───────────────┤
        │               │               │
        ▼               ▼               ▼
    features.py ────────┴───────> overlay.py
        │                               │
        │                               ▼
        ▼                          geometry.py
    utils.py ◄──────────────────────────┘
```

**Legend:**
- `app.py`: Main CLI entry point
- `dataset.py`: Data loading, splitting, ROI extraction
- `features.py`: ORB + BoVW feature extraction
- `train.py`: SVM training with cross-validation
- `infer.py`: Face detection + validation pipeline
- `overlay.py`: Accessory placement + alpha blending
- `geometry.py`: Landmark estimation, pose calculation
- `utils.py`: Shared utilities (I/O, NMS, visualization)

---

## 5. Data Flow (Feature Extraction)

```
┌────────────────┐
│ Raw Image      │
│ (BGR, any size)│
└───────┬────────┘
        │
        ▼
┌────────────────┐
│ Grayscale      │
│ (single channel│
└───────┬────────┘
        │
        ▼
┌────────────────┐
│ Resize 128×128 │
│ (standardize)  │
└───────┬────────┘
        │
        ▼
┌────────────────────┐
│ ORB Detect         │
│ → List[KeyPoint]   │
│ → ndarray (N×32)   │
└───────┬────────────┘
        │
        │ N descriptors × 32 bytes
        ▼
┌────────────────────┐
│ K-Means Assign     │
│ • Load codebook    │
│ • Predict cluster  │
└───────┬────────────┘
        │
        │ Cluster labels [0..255]
        ▼
┌────────────────────┐
│ Build Histogram    │
│ • bincount(256)    │
│ • L1 normalize     │
└───────┬────────────┘
        │
        │ 256D vector
        ▼
┌────────────────────┐
│ StandardScaler     │
│ • Center (μ=0)     │
│ • Scale (σ=1)      │
└───────┬────────────┘
        │
        │ Normalized features
        ▼
┌────────────────────┐
│ SVM Input          │
│ (ready for pred)   │
└────────────────────┘
```

---

## 6. Accessory Overlay Positioning

```
                        Face Bounding Box (x, y, w, h)
                     ┌────────────────────────────┐
                     │                            │
                     │   ┌──────────────┐         │
                     │   │     HAT      │◄────────┼─ scale: 1.2×w
  rotation: eye angle│   │  (rotated)   │         │  offset: -0.25h
                     │   └──────┬───────┘         │
                     │          │                 │
                     │    ┌─────┴──────┐          │
   left_ear_x ──────►│ ◄──┤            │──► ◄────┼─ right_ear_x
   (cx - 0.45w)      │ 👂 │  O      O  │ 👂      │  (cx + 0.45w)
                     │    │   EYES     │          │
                     │    │            │          │
                     │    │     💎     │◄─────────┼─ nose piercing
                     │    │   (nose)   │          │  (y = y + 0.58h)
                     │    │            │          │
                     │    │     ─      │          │
                     │    │   (mouth)  │          │
                     │    │            │          │
                     │    │    🎨      │◄─────────┼─ face tattoo
                     │    │  (cheek)   │          │  (x = cx ± 0.35w)
                     │    │            │          │  (y = y + 0.65h)
                     │    └────────────┘          │
                     │                            │
                     └────────────────────────────┘

Legend:
  cx, cy = Face center (x + w/2, y + h/2)
  w, h   = Face width, height
  👂     = Earring position (geometric estimate)
  💎     = Nose piercing (landmark or heuristic)
  🎨     = Face tattoo (cheek placement)
```

---

## 7. Performance Breakdown (Typical Frame)

```
Total Frame Processing Time: ~55ms (18 FPS)
│
├─ Haar Detection ──────────── 20ms (36%)
│  │
│  ├─ Grayscale conversion ─── 1ms
│  ├─ Multi-scale search ───── 15ms
│  └─ Feature detection ────── 4ms
│
├─ SVM Validation ─────────── 12ms (22%)
│  │
│  ├─ ROI extraction ───────── 2ms
│  ├─ ORB features ──────────── 5ms
│  ├─ BoVW encoding ─────────── 3ms
│  └─ SVM predict ──────────── 2ms
│
├─ Post-processing ────────── 8ms (15%)
│  │
│  ├─ NMS ──────────────────── 3ms
│  ├─ Landmark detection ──── 4ms
│  └─ Pose estimation ──────── 1ms
│
├─ Overlay Rendering ──────── 12ms (22%)
│  │
│  ├─ Position calculation ─── 1ms
│  ├─ Image resizing ───────── 5ms
│  ├─ Rotation ──────────────── 2ms
│  └─ Alpha blending ───────── 4ms
│
└─ Display/IO ─────────────── 3ms (5%)
   │
   ├─ imshow() ───────────────── 2ms
   └─ waitKey() ──────────────── 1ms

Note: With --no-svm flag, total time ~35ms (28 FPS)
```

---

**These diagrams illustrate the complete system architecture and data flow through the CV Accessory Overlay pipeline.**
